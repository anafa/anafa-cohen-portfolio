# content/

Reserved for `cv.json` (Part 3 of `docs/PLAN.md`) — the single structured source of truth for site content (name, tagline, bio, roles, skills). `frontend/js/main.js` will `fetch()` this file at runtime; no content is hardcoded into HTML.
